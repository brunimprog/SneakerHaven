import React from 'react';
import { SafeAreaView } from 'react-native';

import NavBar from '../components/navbar'

const Home = () => {
    return (
        <SafeAreaView>
            <NavBar />
        </SafeAreaView>
    );
};

export default Home;